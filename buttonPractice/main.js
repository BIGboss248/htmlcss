function buttonToggle() {
  let buttonElement = document.querySelector(".finishButton");
  if (buttonElement.innerHTML === "Finish") {
    buttonElement.innerHTML = "Finished";
  } else {
    buttonElement.innerHTML = "Finish";
  }
}
